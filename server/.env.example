# App
NODE_ENV=development
PORT=9090
CORS_ORIGIN=*
JWT_ISSUER=https://your-saas.com
JWT_AUDIENCE=https://api.your-saas.com
JWT_JWKS_URL=https://your-auth.com/.well-known/jwks.json  # or use HS256 below
JWT_HS256_SECRET=replace-in-dev-only

# Postgres for SaaS metadata
DATABASE_URL=postgresql://user:pass@host:5432/saasdb?sslmode=require

# OpenAI
OPENAI_API_KEY=sk-...

# Secrets backend (choose one)
SECRETS_BACKEND=env          # env | aws-kms | gcp-kms | vault
# For env backend (dev only)
TENANT_CONN_DEK_BASE64=...   # optional dev-only DEK
